{{- if .Values.heimdall.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "heimdall.fullname" . }}
  labels:
    {{- include "heimdall.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: {{ include "heimdall.fullname" . }}
  minReplicas: {{ .Values.heimdall.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.heimdall.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.heimdall.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.heimdall.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.heimdall.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.heimdall.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
